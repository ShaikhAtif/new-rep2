git status
git init			(to initialize/make the local folder into repo)
git add -A			(to allow git watch/track the files)
git commit -m "Any message"	(to store data)
git config --global user.name "ShaikhAtif"
git config --global user.email "k181122@nu.edu.pk"
git remote add origin "cloud repo link"
git push -u origin master 	(push=local data to cloud, -u=track local and cloud,origin=cloud,master=code on which to be deployed)

